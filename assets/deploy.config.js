// 部署配置：仅用于纯静态托管（如 CloudStudio / 公网静态空间）时的「演示级口令」软屏障。
// 重要：口令写在前端代码里，任何人看源码都能拿到，它只是"非公开转发"的软屏障，
// 不是真正鉴权。要真正登录保护，请用内网 Node 服务（server.js）的访问码 / 企微身份模式。
// 本地 localhost 开发不会触发此屏障（见 login.js）。
window.__DEPLOY__ = {
  passcode: 'huida8131',
  label: '演示口令（非严格鉴权）'
};
